# Gmail SMTP (server-side only, used by api/contact.ts)
GMAIL_USER=
GMAIL_APP_PASSWORD=

# Supabase — service role (server-side only, used by api/contact.ts)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Supabase — public, used by the frontend client (safe to expose)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
